apiVersion: v1
kind: ConfigMap
metadata:
  name: review-service-config
  namespace: ecommerce
data:
  SPRING_CLOUD_CONFIG_URI: http://config-server:8888
  EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka
  SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/review-service?createDatabaseIfNotExist=true
  SPRING_DATASOURCE_USERNAME: root
---
apiVersion: v1
kind: Secret
metadata:
  name: review-service-secret
  namespace: ecommerce
type: Opaque
data:
  SPRING_DATASOURCE_PASSWORD: cm9vdA== # base64 encoded "root"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-service
  namespace: ecommerce
spec:
  replicas: 2
  selector:
    matchLabels:
      app: review-service
  template:
    metadata:
      labels:
        app: review-service
    spec:
      containers:
      - name: review-service
        image: firasmahjoubi/review-service:latest
        ports:
        - containerPort: 8094
        envFrom:
        - configMapRef:
            name: review-service-config
        - secretRef:
            name: review-service-secret
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8094
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8094
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: ecommerce
spec:
  selector:
    app: review-service
  ports:
  - port: 8094
    targetPort: 8094
  type: ClusterIP
