apiVersion: v1
kind: ConfigMap
metadata:
  name: voucher-service-config
  namespace: ecommerce
data:
  SPRING_CLOUD_CONFIG_URI: http://config-server:8888
  EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka
  SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/voucher-service?createDatabaseIfNotExist=true
  SPRING_DATASOURCE_USERNAME: root
---
apiVersion: v1
kind: Secret
metadata:
  name: voucher-service-secret
  namespace: ecommerce
type: Opaque
data:
  SPRING_DATASOURCE_PASSWORD: cm9vdA== # base64 encoded "root"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: voucher-service
  namespace: ecommerce
spec:
  replicas: 2
  selector:
    matchLabels:
      app: voucher-service
  template:
    metadata:
      labels:
        app: voucher-service
    spec:
      containers:
      - name: voucher-service
        image: firasmahjoubi/voucher-service:latest
        ports:
        - containerPort: 8096
        envFrom:
        - configMapRef:
            name: voucher-service-config
        - secretRef:
            name: voucher-service-secret
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8096
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8096
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: voucher-service
  namespace: ecommerce
spec:
  selector:
    app: voucher-service
  ports:
  - port: 8096
    targetPort: 8096
  type: ClusterIP
